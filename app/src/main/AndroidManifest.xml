<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gg">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.activity_login" />
        <activity android:name=".ui.MainActivityChat" />
        <!-- activity android:name=".activity_welcome" -->
        <!-- en mi caso no voy a usar el manifest para iniciar la actividad principal-->
        <!-- Voy a marcar como la activity principal a MainActiviy y desde ella voy a llamar a -->

        <!-- android:name=".activity_register" -->
       <activity android:name=".ui.MainActivity"
            android:label="MyTutorEd">
           <!-- activity_welcome -->
           <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>